import 'package:flutter/material.dart';

class IncomePage extends StatelessWidget {
  static const String routeName = "/income";

  const IncomePage();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Fuck you on income screen")),
    );
  }
}