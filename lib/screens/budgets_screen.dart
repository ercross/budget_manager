import 'package:flutter/material.dart';

class BudgetsPage extends StatelessWidget {

  static const String routeName = "/budgets";

  const BudgetsPage();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Fuck you on budget screen")),
    );
  }
}